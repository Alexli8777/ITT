# Excel 處理工具

這是一個純前端的 Excel 處理工具，使用 SheetJS 實作，可直接部署至 GitHub Pages。

## 功能
- 上傳 Excel 檔案
- 處理資料（範例：將「數值」欄位乘以 2）
- 下載處理後的 Excel 檔案

## 使用方式
1. 開啟 `index.html`
2. 上傳 `.xlsx` 或 `.xls` 檔案
3. 點擊「處理並下載」按鈕
